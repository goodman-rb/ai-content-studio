# 📖 Инструкция по установке AI Content Studio

Подробное руководство по установке и настройке приложения с нуля.

---

## 📋 Оглавление

1. [Системные требования](#системные-требования)
2. [Установка Python и зависимостей](#установка-python-и-зависимостей)
3. [Настройка Google Sheets API](#настройка-google-sheets-api)
4. [Получение DeepSeek API ключа](#получение-deepseek-api-ключа)
5. [Подготовка Google Таблицы](#подготовка-google-таблицы)
6. [Конфигурация приложения](#конфигурация-приложения)
7. [Первый запуск](#первый-запуск)
8. [Решение проблем](#решение-проблем)

---

## 🖥️ Системные требования

- **Операционная система:** Windows 10/11, macOS 10.14+, Linux
- **Python:** версия 3.9 или выше
- **Оперативная память:** минимум 2 GB
- **Интернет:** стабильное подключение
- **Браузер:** Chrome, Firefox, Safari (последние версии)

---

## 🐍 Установка Python и зависимостей

### Шаг 1: Установка Python

**Windows:**
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. ✅ **Важно:** Отметьте "Add Python to PATH"
4. Нажмите "Install Now"

**macOS:**
```bash
# Установка через Homebrew
brew install python@3.11
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3.11 python3-pip python3-venv
```

### Шаг 2: Проверка установки

```bash
python --version
# Должно вывести: Python 3.9.x или выше
```

### Шаг 3: Клонирование репозитория

```bash
git clone https://github.com/your-username/ai-content-studio.git
cd ai-content-studio
```

Если нет Git:
- Скачайте ZIP архив → Распакуйте → Откройте папку в терминале

### Шаг 4: Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

Вы должны увидеть `(venv)` в начале строки терминала.

### Шаг 5: Установка зависимостей

```bash
pip install -r requirements.txt
```

Процесс займет 2-3 минуты.

---

## 🔑 Настройка Google Sheets API

### Шаг 1: Создание проекта в Google Cloud

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите **"Создать проект"**
3. Название: `AI Content Studio` (или любое другое)
4. Нажмите **"Создать"**

### Шаг 2: Активация Google Sheets API

1. В меню слева → **"API и сервисы"** → **"Библиотека"**
2. Найдите **"Google Sheets API"**
3. Нажмите **"Включить"**
4. Повторите для **"Google Drive API"**

### Шаг 3: Создание Service Account

1. **"API и сервисы"** → **"Учетные данные"**
2. **"Создать учетные данные"** → **"Сервисный аккаунт"**
3. Название: `ai-content-bot`
4. Роль: **"Редактор"** (Editor)
5. Нажмите **"Готово"**

### Шаг 4: Получение JSON ключа

1. Найдите созданный сервисный аккаунт в списке
2. Нажмите на него → вкладка **"Ключи"**
3. **"Добавить ключ"** → **"Создать новый ключ"**
4. Тип: **JSON**
5. Файл `credentials.json` скачается автоматически

### Шаг 5: Размещение credentials.json

```bash
# Скопируйте скачанный файл в корень проекта
mv ~/Downloads/ai-content-studio-*.json ./credentials.json
```

**Важно:** Не удаляйте и не публикуйте этот файл!

---

## 🤖 Получение DeepSeek API ключа

### Шаг 1: Регистрация

1. Перейдите на [DeepSeek Platform](https://platform.deepseek.com/)
2. Зарегистрируйтесь (email + пароль)
3. Подтвердите email

### Шаг 2: Создание API ключа

1. Перейдите в раздел **"API Keys"**
2. Нажмите **"Create new key"**
3. Название: `AI Content Studio`
4. **Скопируйте ключ** (показывается только один раз!)

Формат ключа: `sk-...` (64 символа)

### Шаг 3: Пополнение баланса

1. Перейдите в **"Billing"**
2. Пополните на $5-10 (хватит на ~50,000 постов)
3. DeepSeek очень дешевый: ~$0.0001 за пост

---

## 📊 Подготовка Google Таблицы

### Вариант A: Копирование готовой таблицы (рекомендуется)

1. Откройте [шаблон таблицы](https://docs.google.com/spreadsheets/d/11POL8ft8ETDnI-Qhvdw0qSeP8OnPjVx55gzya1dTtEU)
2. **Файл** → **Создать копию**
3. Переименуйте в `AI Content Studio - Салон Шарм`

### Вариант B: Создание с нуля

1. Создайте новую таблицу на [Google Sheets](https://sheets.google.com/)
2. Создайте листы: `Services`, `Discounts`, `General_Info`, `Content_Plan`, `Prompts`
3. Заполните структуру согласно [sheets_structure.md](../google_sheets/sheets_structure.md)

### Шаг 2: Предоставление доступа сервисному аккаунту

1. Откройте `credentials.json`
2. Найдите строку `"client_email": "ai-content-bot@..."`
3. Скопируйте этот email
4. В Google Таблице → **Настройки доступа** → **Добавить людей**
5. Вставьте email сервисного аккаунта
6. Права: **Редактор**
7. Снимите галочку **"Уведомить пользователей"**

### Шаг 3: Получение ID таблицы

URL таблицы выглядит так:
```
https://docs.google.com/spreadsheets/d/11POL8ft8ETDnI-Qhvdw0qSeP8OnPjVx55gzya1dTtEU/edit
                                        ↑______________________________________↑
                                                    Это SHEET_ID
```

Скопируйте ID (между `/d/` и `/edit`).

---

## ⚙️ Конфигурация приложения

### Шаг 1: Настройка secrets.toml

Создайте папку и файл:
```bash
mkdir -p .streamlit
```

**Windows:**
```bash
copy .streamlit\secrets.toml.example .streamlit\secrets.toml
notepad .streamlit\secrets.toml
```

**macOS/Linux:**
```bash
cp .streamlit/secrets.toml.example .streamlit/secrets.toml
nano .streamlit/secrets.toml
```

### Шаг 2: Заполнение secrets.toml

```toml
# DeepSeek API
DEEPSEEK_API_KEY = "sk-ваш-ключ-здесь"

# Google Sheets (опционально, если не хочешь хардкодить в app.py)
SHEET_ID = "11POL8ft8ETDnI-Qhvdw0qSeP8OnPjVx55gzya1dTtEU"
```

**Важно:** 
- Замените значения на реальные
- Не добавляйте пробелы вокруг `=`
- Используйте двойные кавычки `"`

### Шаг 3: Настройка app.py

Откройте `app.py` и найдите строку:
```python
SHEET_ID = "11POL8ft8ETDnI-Qhvdw0qSeP8OnPjVx55gzya1dTtEU"
```

Замените на ID вашей таблицы.

---

## 🚀 Первый запуск

### Шаг 1: Запуск приложения

```bash
streamlit run app.py
```

Вы должны увидеть:
```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

### Шаг 2: Открытие в браузере

Браузер откроется автоматически. Если нет:
- Перейдите на http://localhost:8501

### Шаг 3: Проверка подключения

В правом верхнем углу должно быть:
- ✅ **Зеленый индикатор** — всё работает
- ❌ **Красный** — проверьте credentials.json и доступ к таблице

### Шаг 4: Инициализация таблиц

При первом запуске приложение автоматически создаст:
- Лист `Content_Plan` (если его нет)
- Лист `Prompts` с промптами по умолчанию (если его нет)

Проверьте в Google Sheets — должны появиться новые листы.

---

## 🐛 Решение проблем

### Проблема 1: "credentials.json не найден"

**Решение:**
```bash
# Проверьте наличие файла
ls credentials.json  # macOS/Linux
dir credentials.json # Windows

# Файл должен быть в корне проекта
```

### Проблема 2: "APIError 403: Permission denied"

**Решение:**
1. Откройте Google Таблицу
2. Проверьте доступ для сервисного аккаунта (email из credentials.json)
3. Права должны быть "Редактор"

### Проблема 3: "Module 'streamlit' not found"

**Решение:**
```bash
# Проверьте активацию venv
# Должно быть (venv) в начале строки

# Переустановите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Проблема 4: DeepSeek API ошибки

**Решение:**
1. Проверьте ключ в `.streamlit/secrets.toml`
2. Проверьте баланс на [platform.deepseek.com](https://platform.deepseek.com)
3. Формат ключа: `sk-...` (64 символа)

### Проблема 5: Порт 8501 занят

**Решение:**
```bash
# Запустите на другом порту
streamlit run app.py --server.port 8502
```

### Проблема 6: Листы не создаются автоматически

**Решение:**
1. Создайте листы вручную в Google Sheets
2. Скопируйте структуру из [sheets_structure.md](../google_sheets/sheets_structure.md)
3. Перезапустите приложение

---

## 📞 Поддержка

Если проблема не решена:
1. Проверьте [FAQ в документации](USER_GUIDE.md#faq)
2. Создайте [Issue на GitHub](https://github.com/your-username/ai-content-studio/issues)
3. Приложите:
   - Версию Python (`python --version`)
   - Версию Streamlit (`streamlit --version`)
   - Текст ошибки
   - Скриншот (если возможно)

---

## ✅ Чеклист успешной установки

- [ ] Python 3.9+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Все зависимости установлены
- [ ] `credentials.json` на месте
- [ ] Google Sheets API активирован
- [ ] Сервисный аккаунт имеет доступ к таблице
- [ ] DeepSeek API ключ получен и добавлен в secrets.toml
- [ ] SHEET_ID настроен в app.py
- [ ] Приложение запускается без ошибок
- [ ] Листы Content_Plan и Prompts созданы

Если все пункты выполнены — поздравляю! 🎉

---

**Следующий шаг:** [Руководство пользователя](USER_GUIDE.md)